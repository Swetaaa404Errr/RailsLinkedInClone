<%= form_with(model: user_account_attachment) do |form| %>
  <% if user_account_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_account_attachment.errors.count, "error") %> prohibited this user_account_attachment from being saved:</h2>

      <ul>
        <% user_account_attachment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_account_id %>
    <%= form.number_field :user_account_id %>
  </div>

  <div class="field">
    <%= form.label :certificate %>
    <%= form.text_field :certificate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
